# VideoMAE Requirements
# =====================

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
numpy>=1.21.0
tqdm>=4.64.0
pyyaml>=6.0

# Video loading (install at least one)
opencv-python>=4.5.0
decord>=0.6.0  # Faster alternative to opencv for video loading

# Optional but recommended
einops>=0.6.0
timm>=0.9.0

# For visualization (optional)
matplotlib>=3.5.0
seaborn>=0.12.0

# For logging (optional)
wandb>=0.15.0

